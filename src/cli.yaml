name: mptun
version: "0.1"
author: drblah
about: Creates a multi path VPN tunnel for more reliable networking over unreliable medium like cellular.
args:
  - config:
      long: config
      value_name: CONFIGFILE
      about: Sets the path to the configuration file
      takes_value: true
      required: true
